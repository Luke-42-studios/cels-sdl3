---
phase: 10-example-application
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All v1 features are visually confirmed working in the running demo"
    - "Click-to-place interaction works correctly with colored shapes appearing at click positions"
    - "FPS counter updates in real-time (not frozen)"
    - "Escape key triggers clean shutdown with no crash"
  artifacts: []
  key_links: []
---

<objective>
Run the demo application interactively and verify that all v1 features of cels-sdl3 are working end-to-end.

Purpose: Final validation gate for the entire cels-sdl3 v1 library. Confirms that the composition of all 9 prior phases produces a working, visually correct application.

Output: Verified demo with all features confirmed working. Phase 10 complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-example-application/10-CONTEXT.md
@.planning/phases/10-example-application/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and launch demo</name>
  <files></files>
  <action>
Build the demo target and launch it:

```bash
cd /home/cachy/workspaces/libs/cels-sdl3/cmake-build-debug
cmake .. && cmake --build .
./demo
```

The demo should open a 1280x720 window showing the landscape scene. Let it run for the verification step.
  </action>
  <verify>
Window opens without crash. Console shows no error messages.
  </verify>
  <done>Demo binary runs and window appears.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete cels-sdl3 v1 demo application exercising all features: window management, input handling, draw primitives (filled rect, outlined rect, line), texture loading/rendering, and text rendering with caching.
  </what-built>
  <how-to-verify>
Run the demo from the build directory:
```bash
cd /home/cachy/workspaces/libs/cels-sdl3/cmake-build-debug && ./demo
```

Verify each feature visually:

1. **Window**: 1280x720 window opens with sky-blue background
2. **Draw primitives**:
   - Green ground fills the bottom portion of the screen
   - Horizon line separates sky and ground
   - A building (brown filled + outlined rect) appears in the scene
   - A sun (yellow outlined rect) appears in the upper-right area
3. **Texture**: A tree sprite (pixel-art PNG) renders in the scene
4. **Text overlays** (top-left corner):
   - "cels-sdl3 Demo" title in white
   - "FPS: XX" counter in yellow -- should update continuously (not frozen)
   - "Click to plant / ESC to quit" instructions in gray
5. **Input - mouse**: Click anywhere in the window -- a small colored square should appear at the click position. Click multiple times -- shapes should accumulate with cycling colors (red, green, blue, yellow, magenta, cyan)
6. **Input - keyboard**: Press Escape -- the window should close cleanly
7. **Clean exit**: Console should print "cels-sdl3 Demo -- clean exit" with no crash or error messages
  </how-to-verify>
  <resume-signal>Type "approved" if all features are working correctly, or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
All 7 verification points from the checkpoint confirmed by human tester.
</verification>

<success_criteria>
- Human confirms all v1 features visible and working in the running demo
- No visual artifacts, crashes, or error messages
- Clean exit on Escape
- Phase 10 and the entire cels-sdl3 v1 roadmap complete
</success_criteria>

<output>
After completion, create `.planning/phases/10-example-application/10-02-SUMMARY.md`
</output>
